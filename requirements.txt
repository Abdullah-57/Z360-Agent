# Direct dependencies only — the packages this app actually imports.
# Kept as a top-level list (not a full `pip freeze`) so the installer can
# resolve compatible transitive versions itself. The previous frozen file
# pinned unused Gradio + Google packages (google-api-core / grpcio-status /
# google-ai-generativelanguage) whose pins were mutually unsatisfiable on a
# clean build — none of them are imported by this app, so they're gone.
# fastapi[standard] (not bare fastapi) bundles the `fastapi run` CLI and
# uvicorn[standard] that FastAPI Cloud uses to launch the app.
fastapi[standard]==0.141.1
pydantic==2.13.4
python-dotenv==1.2.2
groq==0.37.1
deepagents==0.7.0
langchain-groq==1.1.3
langchain-core==1.5.2
langgraph==1.2.10
supabase==2.31.0
